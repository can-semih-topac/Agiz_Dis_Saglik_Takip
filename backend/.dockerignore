# Yerel Windows derlemesinden kalan klasörler container'a kopyalanmasın — içlerinde
# Windows'a özel mutlak yollar barındıran NuGet önbelleği var, container'da restore'u bozuyor.
**/bin/
**/obj/
ci-build/
.vs/

# Gerçek kullanıcı yüklemeleri (durum notu fotoğrafları vb.) — bunlar image'a değil,
# docker-compose.yml'deki "backend-uploads" volume'üne ait. Buraya girerse her image
# derlemesinde o an host'ta ne varsa image'ın içine donmuş halde gömülür.
src/AgizDisSaglikTakip.WebAPI/wwwroot/uploads/
